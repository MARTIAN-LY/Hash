package com.example.hash.prepare

class Something {
    /*
    来自RecyclerView

    //初始数据源
        var list = mutableListOf(
            HomeItem("起床", true),
            HomeItem("吃饭", true),
            HomeItem("学习", false),
            HomeItem("敲代码", true),
            HomeItem("玩游戏", false),
            HomeItem("睡觉", false)
        )
        //给adapter传递数据
        var adapter = HomeRecyclerAdapter(list);

        //给RecyclerView设置adapter 和 layoutManager
        binding.homeRecyclerView.adapter = adapter
        binding.homeRecyclerView.layoutManager = LinearLayoutManager(activity)


        //新增
        binding.testButton.setOnClickListener {
            val title = binding.testEdit.text.toString()
            list.add(HomeItem(title, false))
            adapter.notifyItemInserted(list.size - 1)
        }
*/


}